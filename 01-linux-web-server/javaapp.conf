server {
    listen 80;
    server_name up4soft.julia-b.work www.up4soft.julia-b.work;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name up4soft.julia-b.work www.up4soft.julia-b.work;

    ssl_certificate     /etc/letsencrypt/live/up4soft.julia-b.work/fullchain.pem;  # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/up4soft.julia-b.work/privkey.pem;    # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf;                               # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;                                 # managed by Certbot

    root /var/www/javaapp/build;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8080; 
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_buffering off;
    }
}

